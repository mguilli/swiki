%h4 
  Wiki Owner:
= @owner.name
= @owner.email

%h4 Current Collaborators
- @current.each do |user|
  = user.name
  = user.email
  = link_to "Remove", wiki_collab_path(@wiki, id: Collab.collab_id(@wiki, user)), method: :delete
  %br

%h4 Available Collaborator
- @available.each do |user|
  = user.name
  = user.email
  = link_to "Add", '#'
  %br
